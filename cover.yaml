- platform: template
  covers:
    garage_door:
      device_class: garage
      friendly_name: "Garage Door"
      open_cover:
        service: script.garage_door_trigger
      close_cover:
        service: script.garage_door_trigger
      icon_template: >-
        {% if is_state('binary_sensor.fgk10x_door_opening_sensor_sensor', 'off')%}
        mdi:garage-open
        {% else %}
        mdi:garage
        {% endif %}
      value_template: "{{ is_state('binary_sensor.fgk10x_door_opening_sensor_sensor', 'on') }}"